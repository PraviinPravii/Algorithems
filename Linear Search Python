<pre>
<code>
class Solution:
    # @param A : list of integers
    # @param B : integer
    # @return an integer
    def linearSearch(self, A, B):
        for i in range(len(A)):#Lopp of the Array
            if A[i]==B:        #Comparision
                return 1       #found #return 1
                break          #IF found Break 
        else:
            return 0           #Not found return 0

</code>
</pre>
